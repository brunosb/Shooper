<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.developer.barbosa.shopper.activity.MenuActivity"
    android:background="@drawable/background_app"
    android:orientation="vertical"
    android:weightSum="1">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_logo"
        android:layout_gravity="center"
        android:layout_marginBottom="60dp"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:layout_weight="0.3" />


    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/button_menu"
        android:shadowColor="#A8A8A8"
        android:shadowDx="0"
        android:shadowDy="0"
        android:text="@string/lbl_btn_menu_responder"
        android:textColor="#FFF"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_gravity="center_horizontal|center_vertical"
        android:drawableLeft="@drawable/ic_btn_responder"
        android:layout_marginBottom="10dp"
        android:id="@+id/btnResponderPerguntas"
        android:onClick="responderPerguntas"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/button_menu"
        android:shadowColor="#A8A8A8"
        android:shadowDx="0"
        android:shadowDy="0"
        android:text="@string/lbl_btn_menu_trocar_pontos"
        android:textColor="#FFF"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_gravity="center_horizontal|center_vertical"
        android:drawableLeft="@drawable/ic_btn_trocar"
        android:id="@+id/btnTrocaPontos"
        android:onClick="trocarPontos"/>

</LinearLayout>
